# Vite environment variables MUST start with VITE_ to be exposed to the browser
# (replaces CRA's REACT_APP_* prefix)

VITE_API_URL=http://localhost:3001/api
VITE_WS_URL=http://localhost:3001
VITE_APP_NAME=DevSpace
